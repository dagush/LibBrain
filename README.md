# LibBrain
Modules to be used together with Neuronumba (https://github.com/neich/neuronumba)
Mainly routine, but useful codes! ;-)

# installation:
The package is not in the pip repository, but temporarily you can install it with:

`pip install -e "git+https://github.com/dagush/LibBrain.git#egg=LibBrain"`
